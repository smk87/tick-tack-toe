import { createStyles, makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() =>
	createStyles({
		mainMenu: {
			width: '25rem',
		},
		startGame: {
			width: '12rem',
			height: '3rem',
		},
	})
);
