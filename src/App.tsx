// Library imports
import React, { ReactElement } from 'react';

// File imports
import { Router } from 'router';

export const App = (): ReactElement => {
	return (
		<div className='App'>
			<Router />
		</div>
	);
};
